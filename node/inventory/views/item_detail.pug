extends layout

block content
	h1= item.name
	p= item.description
	p= item.category.name
	p= item.price
	p= item.quantity
	
	hr
	
	a(href=item.url + '/update') update
	br
	a(href=item.url + '/delete') delete
